{
  "name": "tsparticles-utils-root",
  "private": true,
  "version": "3.4.7",
  "scripts": {
    "build": "nx run-many -t build",
    "build:ci": "nx run-many -t build:ci",
    "build:affected": "nx affected -t build",
    "version:alpha": "nx release version prerelease --preid=alpha --all",
    "version:beta": "nx release version prerelease --preid=beta --all",
    "version:patch": "nx release version patch --all",
    "version:minor": "nx release version minor --all",
    "version:major": "nx release version major --all",
    "publish:alpha": "nx release publish --tag=alpha",
    "publish:beta": "nx release publish --tag=beta"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@nx/js": "^22.7.0",
    "@nx/devkit": "^22.7.0",
    "nx": "^22.7.0",
    "nx-cloud": "^19.1.3",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.33.2"
}