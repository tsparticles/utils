{
  "name": "tsparticles-utils-root",
  "private": true,
  "version": "3.4.7",
  "scripts": {
    "build": "nx run-many -t build",
    "build:ci": "nx run-many -t build:ci",
    "build:affected": "nx affected -t build",
    "version:alpha": "nx release version prerelease --preid=alpha --projects=*",
    "version:beta": "nx release version prerelease --preid=beta --projects=*",
    "version:patch": "nx release version patch --projects=*",
    "version:minor": "nx release version minor --projects=*",
    "version:major": "nx release version major --projects=*",
    "publish:alpha": "nx release publish --tag=alpha",
    "publish:beta": "nx release publish --tag=beta"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@nx/js": "^22.7.0",
    "@nx/devkit": "^22.7.0",
    "nx": "^22.7.0",
    "nx-cloud": "^19.1.3",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.33.2"
}